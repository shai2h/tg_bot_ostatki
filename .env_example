DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_PASS=change_me
DB_NAME=tg_bot_ostatki

MAX_TOKEN=
BOT_TOKEN=
BOT_RUN_MODE=polling

API_HOST=0.0.0.0
API_PORT=8000

ONE_C_STALE_AFTER_SECONDS=300
LOG_LEVEL=INFO

APP_PUBLISH_PORT=8000
DB_PUBLISH_PORT=5432
POSTGRES_VOLUME_NAME=tg_bot_ostatki_pgdata

# Optional webhook mode (do not switch without verifying handlers parity)
# MAX_WEBHOOK_URL=
# MAX_WEBHOOK_SECRET=

# Legacy monitoring (not used by Docker app container)
# MONITOR_BOT_TOKEN=
# MONITOR_CHAT_ID=
